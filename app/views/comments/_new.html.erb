<div class="reply">
    <%= form_for([article, comment]) do |f| %>
        <%= render :partial => "share/error_message", :locals => {:target => comment} %>
        <div class="field">
            <%= f.hidden_field :parent_id, :value => params[:parent_id] %>
            <%= f.label :body, "回复" %><br />
            <%= f.text_area :body, :cols => 80, :rows => 20 %>
        </div>
        <div class="actions">
            <%= f.submit("发表") %>
        </div>
    <% end %>
</div>

