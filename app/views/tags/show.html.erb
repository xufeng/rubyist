<% content_for(:title) do %>
  <%= @tag.name %> 
<% end %>
<h1><%= @tag.name %></h1>
<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article) %>
    </li>
  <% end %>
</ul>
<%= paginate @articles %>
